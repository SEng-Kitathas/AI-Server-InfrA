# A-042 Historical WIP Recovery Pointer

Status: **HISTORICAL RECOVERY EVIDENCE / NOT CURRENT SOURCE**

The former `handoff/current/wip/` payload was a frozen A-042 recovery mirror, not an active source fork.
Its exact historical recovery evidence now lives at:

`handoff/archive/a042_wip_recovery_2026-09-07/`

The archive retains `A042_WIP_INVENTORY.json` and all 13 payload members with their original bytes and SHA-256 identities.

Do not merge or restore these historical bytes over newer production source without first localizing current worktree state.
