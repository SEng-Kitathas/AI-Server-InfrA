# PCMMAD Receiver V30 — Current State

Last updated: 2026-09-06 07:12 ET
Continuity status: **REPAIRED / HANDOFF READY**
Active project mode on resume: **BUILD-COMMIT / AUDIT**
Recommended resume role: **R5 Reality Pressure Engine**

## ICF-CS qualified authority update

The uploaded `RAHL_ENGINEERING_CANONICAL_SOP_R4_4_ICF_CS_V1_0_ADDENDUM_2026-09-05.zip` has been externally qualified on exact bytes and is now **binding additive continuity/process doctrine above sealed R4.4**, within its stated scope ceiling.

Qualified outer ZIP SHA-256: `51ed7f424dff7c67534f7e53e8a7c10ebd93bf33ea575eea49dc3f05c24a92a9`.
Exact registered standard SHA: `b22b6ba65992bf8e235df9acfb044da772282ae3820a1359af3b5309d1e1051b`.
Qualification receipt: `checkpoints/ICF_CS_ADDENDUM_QUALIFICATION_RECEIPT.md`.
Current canonical SOP pointer: `state/doctrine_snapshot/CURRENT_CANONICAL_SOP_POINTER.md`.

`CURRENT_INGRESS_POINTER != CURRENT_STATE_PROOF`.

## Project / mission

Project: `PCMMAD_RECEIVER_LAB`
Root: `E:\new pc\AI_Pushes_Sandbox\projects\PCMMAD_RECEIVER_LAB`

Mission: hostile-modernize V29 PCMMAD Receiver into a production-worthy Laboratory Runtime whose canonical state/durable capability are server-native and transport-independent, with MCP/OpenAPI/CLI/HUD as projections and OBE/Skills as reusable AI-side operating intelligence.

## Foundational law

**AXIOM-01 — INTENT IS A CONSTRAINT, NOT A CEILING.**

Supporting architecture laws:
- TRANSPORT IS NOT ARCHITECTURE AUTHORITY.
- LONG-RUNNING WORK SHALL NOT REQUIRE LONG-RUNNING MODEL ATTENTION.
- RETURN DECISION INFORMATION EARLY; STORE BULK WORK LOCALLY.
- TOOL SUCCESS != TASK SUCCESS.
- MECHANISM FIRST. REPRESENTATION SECOND.
- DONOR != AUTHORITY.
- EMERGENT CAPABILITY IS ALLOWED; EMERGENT AUTHORITY IS NOT.
- BUILD != EMBODY.

Canonical build metabolism:
FIX INTENDED OUTCOME → inspect reality/ancestry/constraints → composition before invention → PROBE → DERIVE → VERIFY → BUILD → ATTACK → ISOLATE → STRIP/MUTATE → EMBODY → RECURSE → CSC/release gate.

## Baseline / separation

Immutable V29 RC1:
- `V29_BASELINE\PCMMAD_RECEIVER_V29_NATIVE_PROTOCOL_RC1.zip`
- SHA-256 `5baf9b66a9137d950aeabac2ea76fbbb919e177f90788e5341ee6e15a80b89d8`

V30 work body:
- `V30_WORKING\PCMMAD_receiver`

Live install remains separate:
- `C:\Users\ancal\Desktop\PCMMAD_RECEIVER_V29_NATIVE_PROTOCOL_RC1\PCMMAD_receiver`

No V30 live promotion has occurred.

## Fresh verified runtime truth

### Live runtime
Verified health 2026-09-05 during checkpoint:
- status online
- scheduler alive
- artifact registration enabled
- root `E:\new pc\AI_Pushes_Sandbox`
- projects root `E:\new pc\AI_Pushes_Sandbox\projects`
- 16 capability families
- family-card counts sum to 96 live tools
- compact imported/action surface remains 30 operations and is not the server ceiling.

### V30 working tree
- **100 native tools / 16 families**
- **245 tests collected** after HUD availability + projection-currentness qualification
- **full suite GREEN** with only the existing conditional Windows symlink-privilege skip
- focused HUD security/approval cluster: **13/13 PASS** (`test_hud_host_guard.py` + `test_hud_bound_approval.py`).

HUD 421 blocker resolution:
- root cause was pre-route DNS-rebinding Host guard representation mismatch, not Runtime bound-approval semantics;
- Flask test client sends bare `Host: localhost`; HUD trusted only loopback identities carrying explicit configured `:5090`;
- guard now accepts only exact loopback identities (`localhost`, `127.0.0.1`, `[::1]`) either bare or with configured HUD port;
- foreign hosts and loopback hosts carrying an explicitly wrong port still return HTTP 421;
- Runtime approval model was not weakened or rewritten.

Verified code identities:
- `operator_hud/server.py` SHA-256 `82d9a12961bcede6986002dfd11a80a2910c6bafb1675fe5a10e20fb74b72a42`
- `tests/test_hud_host_guard.py` SHA-256 `4bc76af3d9e8d5052b060a6b8f31e881d40463d52e3b6f0392071dc4bb7734b7`.

## Major verified V30 survivors

1. **Async authority:** canonical durable scheduler is sole admission/lifecycle authority; small lab execution path delegates.
2. **Concurrency:** cross-project/global-lock contention reproduced and first-layer repaired.
3. **Restart-safe ownership:** one-job runner + Windows Job Object ownership handshake; runner is actuator, not scheduler; venv launcher PID distinguished from actual worker.
4. **Service restart:** canonical PowerShell controller + thin BAT; exact receiver identity protects RAHL-HUD; ngrok convergence; environment rehydration; inline restart delegates to same lifecycle.
5. **Transfer:** ticketed resumable/hash-verified transfer family; cross-volume staging defect fixed; live-only transfer/browser.upload recovered into V30.
6. **Health:** optional browser outage no longer collapses aggregate lab health.
7. **Semantic:** real data/index/model assets recovered, but no qualified executor exists; capability truth is typed `unavailable`, not fake green. Donor retriever SHA `0c0841230d33e427892ca83520f0845ab12c38290d4d9511f1cadce44c564dac`.
8. **Bounded control:** `execution.wait` bounded/capped; `execution.progress` provides decision-grade progress; bulk logs separate.
9. **Batch/results:** oversized batch results use server-side handles; result retrieval has auto/metadata/preview/range/explicit-full modes.
10. **Capability contracts:** capability/schema versions, schema hash, contract digest, output schema, effective approval, side-effect class, effect traits.
11. **MCP projection:** MCP-ready manifest carries native currentness/authority/effects and manifest digest.
12. **Approval authority:** exact capability/contract/argument/target-bound, short-lived, integrity-checked, single-use challenges; authority envelope separate from capability args; legacy inline path transitional only.
13. **Context:** line-window 500 fixed; physically bounded file/search/rehydration reads; exact project-root scope; relative/absolute and real Windows junction escapes rejected; strict UTF-8 byte budgets.
14. **Git:** exact repo grounding; no ancestor-repo mutation; unborn repos valid; commit/reset/clean verify postconditions; read failures remain compatible and grounded.
15. **AXIOM-01:** compact constitutional seed emitted through `/health` and native `lab.health`.
16. **Runtime/adapter contract:** compatibility contract + human/machine projection matrix exist.
17. **HUD design authority:** Aero-Glass/Liquid-Aero, HoloFont, Ui/YuiUI/CogTerm, three-wing cockpit, Forge/Singularity Works donor lineage recovered.
18. **Architectural Mechanism Quarry:** governed donor research branch adopted.

### Verification / shared subprocess hardening
- shared synchronous subprocess capture now spools stdout/stderr to temporary files and reads only bounded prefixes; multi-megabyte child output no longer has to materialize in Python heap merely to return a bounded envelope;
- `verify.gates` remains high-risk/approval-required execution and is exact-project-cwd scoped;
- max 32 gates per synchronous call; per-gate timeout max 600s;
- verifier contract is typed and explicitly states its claim ceiling: command-exit evidence only, not automatic higher-order semantic truth;
- requested/attempted/stopped-early/duration/evidence-scope fields are returned;
- focused verification/subprocess tests pass and full 195-test suite is green.

### Reconciled earned planes recovered from the audit ledger
A focused 45-test reconciliation on 2026-09-05 proved that several later audit slices were already embodied in the current V30 tree even though continuity had still listed them as open:

- **Research plane:** thread-safe 64 MiB bounded cache; streamed 2 MiB arXiv body cap; bounded query vocabulary/results; hunt wall budget default 20s / server cap 30s; partial evidence preserved with structured query errors; typed empty-result handling; starmap accepts typed/mapping hits.
- **Protocol plane:** observation is non-initializing; absent protocol remains absent; guards do not create GENESIS; current GENESIS protocol version carries AXIOM-01 and the full current build metabolism without rewriting historical ledgers.
- **SOP/governed ingestion:** Merkle parser fixed; reads do not mkdir; per-corpus in-process lock; package preflight ceilings; streaming archive SHA; source-currentness revalidation; exact chunk SHA required; unacked chunk replay idempotent; final guard distinguishes registered/verified/current/started/complete/proceed-allowed; `next_chunk` effect truth corrected to mutation.
- **Transfer plane:** exact project scope; API-key auth on direct HTTP routes; mutating HTTP adapter delegates through native bound-authority dispatch; ticket manifest integrity; destination identity binding; exact chunk SHA; full-hash verify + atomic finalize; concurrency/expiry/tamper hostile tests.
- **Plugin plane:** transactional isolated candidate registry; source SHA/currentness; collision rejection; rollback on failed load; stale-tool cleanup; stable bookkeeping references; registry/reload locks; generation + contract digest + changed receipt; health currentness.

Focused reconciliation result: **45/45 PASS** across the exact research/protocol/SOP/transfer/plugin hostile suites. These are current-tree survivors, not merely historical report claims.

Residual claim ceilings remain visible in Revisit/Trace, especially SOP true streaming member inspection/multi-process locking, transfer immutable export snapshots, and plugin sandbox/isolation.

### Web/public-network plane
- public `web.fetch` is qualified for current credential-free external-read scope: public-network-only DNS validation, mixed-answer rejection, redirect/final-target revalidation, physically bounded body reads, URL/user-agent bounds, embedded credential rejection, explicit redirect ceiling, HTTP status, DNS validation timestamps, and separate raw/text truncation facts;
- typed machine contract exposes these bounds/currentness/effects;
- DNS rebinding TOCTOU is explicitly disclosed because stdlib urllib resolves again at connection time; no false socket-pinning claim is made;
- focused web qualification 14/14 PASS; full 203-test suite green.

### Native capability effect contract completion
- current 99-tool registry now has **zero** unknown side-effect classes and **zero** empty effect-trait sets;
- previously ambiguous browser reads, health probes, semantic dependency probes, filesystem/project reads, context cache reads, and service status tools now advertise mechanism-derived effects;
- focused classification/regression cluster: 34/34 PASS; full suite green across 216 collected tests;
- this closes current effect classification only. Generic schema counts remain evidence for later contract/projection work and do **not** authorize premature final schema redesign.

### Native capability availability/currentness
- **REGISTRATION != CAPABILITY_AVAILABILITY != TARGET_HEALTH.**
- Tool cards now expose probe-free static availability contracts; discovery does not execute provider health checks.
- resident mode means a handler is registered/callable, not that arbitrary arguments or the managed target will succeed.
- dynamic mode requires an explicit bounded currentness probe.
- new `lab.capabilities.availability` checks only explicitly requested capabilities (max 32), never invokes target capabilities, and deduplicates shared provider probes.
- provider exceptions return UNKNOWN, never fake green.
- browser capabilities currently share one `browser_bridge` provider; semantic search has a scoped `semantic.monster.default_runtime` provider.
- direct current readback: browser bridge unavailable; semantic default runtime unavailable (`no_qualified_python_runtime`); resident `lab.health` available; 5 requested tools required exactly 2 provider probes.
- focused availability suite 9/9 PASS; adjacent currentness/authority cluster 60/60 PASS; full V30 suite **232 collected tests GREEN**.

### Git publication boundary
- V30 source/evidence body is published to `https://github.com/SEng-Kitathas/AI-Server-InfrA.git` on `main`.
- fresh local/remote readback before this handoff pass: `f63f31f7f2ae89f9253d21609a1d431df29e0a69`; tree `6dbdc6fa9a42531ae0252b634cfd3f8e0d749ba3`; upstream clean.
- `f63f31f` includes the A-030 projection-currentness reconciliation after the HUD publication commit.
- current V30 suite on those exact bytes: **245 collected tests GREEN** with the existing conditional Windows symlink-privilege skip.
- local self-test/sync/cache/backups/runtime journals remain excluded; `.gitattributes` uses `* -text` to prevent silent normalization of byte-sensitive lineage.
- publication receipt: `checkpoints/GIT_PUBLICATION_CURRENT.md`.
- **GIT_PUBLICATION != LIVE_RUNTIME_PROMOTION.** Desktop live receiver remains separate/unpromoted.
- current user-directed publication task: add a Git-contained ICF-CS recovery snapshot so a fresh thread can rehydrate even when the outer project store is unavailable. This snapshot is a recovery mirror, not a second live authority.

### A-029 MCP availability projection parity
- MCP manifest now preserves native `availability` contracts verbatim and remains probe-free.
- dynamic browser/semantic provider identity/scope survive projection without manufacturing current probe results.
- focused MCP/availability/effect/compact-schema parity cluster: **27/27 PASS**.
- complete V30 suite on current bytes: **233 collected tests GREEN**.
- HUD `/api/tools` already forwards native cards without truncation; presentation of dynamic availability remains open.
- compact OpenAPI `/lab/tools` carries richer cards permissively but does not strongly enumerate availability; this is validation/documentation lossiness, not wire truncation.
- projection matrix/compatibility contract reconciled: completed effect/approval/result/plugin/availability items removed from the open list.

### A-030 HUD availability/core-vs-optional presentation parity
- HUD transport already preserved native availability; operator presentation now distinguishes resident/dynamic/unknown/unavailable state.
- selected dynamic availability is checked only by explicit one-tool native `lab.capabilities.availability` dispatch; catalog load remains probe-free.
- browser bridge is explicitly optional to core HUD/Receiver/journal readiness; browser outage yields `CORE READY · OPTIONAL DEGRADED`, not total-control-plane failure.
- explicit probe evidence is ephemeral UI state bound to native `contract_digest`; stale rows are discarded and selected cards rebind to fresh catalog contracts.
- final HUD+projection currentness cluster: **32/32 PASS**; complete V30 suite **245 collected tests GREEN**.
- no Runtime availability semantics changed; this is projection/presentation hardening only.

## Architecture authority

Locked model:
- PCMMAD = methodology
- Laboratory Runtime = truth + state + deterministic/durable capability
- Adapters = MCP/OpenAPI/CLI/HUD/future transports
- OBE + Skills = reusable AI/operator intelligence
- Model = replaceable co-processor
- Project Chat = replaceable mission surface.

No adapter may own a second durable truth for jobs/projects/results/artifacts/processes/approvals.

OBE/Skill research may proceed now, but final interface promotion waits for reconciliation against the audited successor runtime.

Shared contract artifacts:
- `V30_WORKING\PCMMAD_receiver\reports\PCMMAD_RUNTIME_ADAPTER_COMPATIBILITY_CONTRACT_V0_1.md`
- `V30_WORKING\PCMMAD_receiver\reports\PCMMAD_RUNTIME_ADAPTER_PROJECTION_MATRIX_V0_1.md`
- `.json` companion.

## Holonic/ECS-ish direction

No ECS framework rewrite.
Normalize already-present bones incrementally:
- stable identities/entities
- orthogonal components/state/traits
- systems acting on compatible state
- holons as qualified wholes that may compose upward.

Higher-order capabilities may emerge from primitives, but must earn their own authority/effects/currentness/resources/failure/verification/result contract.

## Mechanism Quarry

Active donor families include ECS, holonic systems, entity/component relations, capability security, effect systems, blackboards, actor/supervisor systems, self-description, hypermedia affordances, dynamic service composition, reflection, typed-state planning, resource queries, and graph/query systems.

Donor mechanisms are dispositioned REJECT / DEFER / QUARRY / CANDIDATE / EARNED after hostile comparison against incumbent PCMMAD mechanism.

## Operator HUD direction

RAHL-HUD is operator/presentation lineage, not Runtime truth authority.

Required presentation donor language:
- Aero-Glass / Liquid-Aero
- HoloFont
- Ui / YuiUI
- CogTerm
- three-wing cockpit
- top corona / bottom command dock
- keyboard-first command surface
- queue/job/progress/topology
- restart/transfer/result/continuity/Git/project views.

Current priority is approval-authority correctness before visual embodiment.

## Final schema — explicitly deferred

Do NOT start final schema redesign on re-entry.

Only after whole-runtime convergence and explicit user “hells yeah, ready” trigger:
- begin a dedicated fresh research/hostile campaign;
- use surviving native holonic/ECS-like runtime contracts as source authority;
- treat JSON Schema/OpenAPI/MCP as validation/projection leaves rather than ontology;
- quarry effect/capability/reflective/hypermedia/planning/graph/resource mechanisms;
- optimize typed lawful composability, currentness, bounded AI context, authority, and verification.

## Continuity repair result

Pre-repair:
- required state and shadow directories existed but were empty;
- project manifest and ledger were absent.

Now verified:
- project manifest exists
- project ledger exists
- registered Current State, **current Commander’s Intent checkpoint**, Next Steps, Doctrine Snapshot, Revisit Ledger, Trace Matrix, Live Shadow, Design Thread Stream, checkpoint, and maintenance note exist with hashes.
- `rehydrateProjectContext(path="continuity")` selects BOTH `continuity.live_shadow` and `continuity.design_thread_stream` with **no open seams**.

ICF-CS continuity status:
- **Intent–Constraint–Frontier Continuity Standard (ICF-CS) v1.0 is now active and registered**;
- generic standard: `state/doctrine_snapshot/INTENT_CONSTRAINT_FRONTIER_CONTINUITY_STANDARD.md`;
- discoverable ingress: `checkpoints/ICF_CS_CURRENT.md`;
- V30 root rehydration now seeds Current → ICF Standard → Intent → Next/Doctrine/Revisit/Trace → Live Shadow → DTS before topic evidence;
- default recursive context excludes transient sync/VCS/cache/venv/backup/scratch noise while explicit targeting remains available;
- index cache currentness is canonical path + size + `mtime_ns`, not TTL-only;
- direct V30 readback against this project returned all nine ICF authority classes, unique paths, `open_seams=[]`, and no sync/scratch noise.

## Current frontier / exact resume point

A-036 post-partition execution lifecycle boundedness is **DERIVED / NO EXECUTION-CODE MUTATION EARNED** and pending this audit step's mandatory Git publication.

A-036 outcomes:
- terminal retention = **DEFER / policy-only**, because A-035 already removed terminal history from scheduler/capacity hot scans;
- default drain = **NO DEFECT** under current global concurrency 8: candidate populations 100/1,000/10,000 all started/read exactly 8 when capacity empty and 0 when saturated;
- configured per-project-full traversal remains linear in candidate count but 10,000 skipped candidates took ~0.776 ms and only one job-file read; no bespoke patch earned;
- `/execution/list(limit=20)` = **REAL BOUNDED-MATERIALIZATION DEFECT**: 100/1,000/10,000 history candidates caused 100/1,000/10,000 `stat()` calls before only 20 records were loaded. This is handed forward to the incremental-derived-state raid rather than patched with an O(history) heap/sort variant.

New cross-domain evidence packet `CROSS DOMAIN RAIDS.md` was read in full and preserved by receipt at `reports/hostile_inputs/CROSS_DOMAIN_RAIDS_2026-09-06_RECEIPT.md` (source bytes 20,441; SHA `dcdafb71cd5a5a66b18139b4cada42dd131fa7a1eb1e01ff117afa5acf310027`).

Independent current-V30 protocol-store reproduction confirms the same implementation disease in a second subsystem:
- 100 events -> 10.421 ms append; 201 events verified; 101 folded;
- 1,000 -> 26.411 ms; 2,001 verified; 1,001 folded;
- 4,000 -> 81.705 ms; 8,001 verified; 4,001 folded.

Promoted hostile audit lens:
**DERIVED_STATE_IS_A_FOLD_NOT_A_REBUILD**.
Meaning: when steady-state mutation can lawfully extend previously verified derived state, extend the fold/checkpoint/cache; full reconstruction/full verification remain recovery/audit backstops.
Companion anti-overreach: **INCREMENTAL_STEADY_STATE != NO_FULL_RECOVERY_PATH**.

Immediate sequence:
1. publish A-036 audit/raid intake to Git and remote-read;
2. A-037 derive/embody protocol-store incremental snapshot + verified-head/checkpoint semantics from current ledger invariants;
3. then deterministic simulation/state-machine on-ramp before larger scheduler concurrency changes;
4. informer watch/cache + slow resync on A-035 active store;
5. Merkle proof-carrying protocol receipts after ledger checkpoint/head semantics stabilize;
6. small high-value raids: lab/protocol idempotency and Windows Job Object resource limits;
7. remaining raid candidates stay provisional until current-tree discriminators.

## Remaining major seams

Known genuinely-open or release-gated areas before reconciliation finishes:
- remaining adapter/projection parity against native effect+availability+authority/currentness contracts;
- async malformed/stale-state/race/final release qualification;
- control/HUD/service-constellation and process-identity hardening;
- semantic executor restoration/qualification;
- transfer immutable-export-snapshot question and final release pressure;
- SOP true streaming/multi-process claim ceilings only if workload/threat evidence demands them;
- plugin sandbox isolation only if plugin threat model changes;
- process identity / node / resource model maturation;
- multi-project/session/operator ergonomics;
- live/V30 capability parity, clean extraction/install/restart, CSC, packaging, promotion;
- OBE/Runtime projection reconciliation;
- HUD visual embodiment;
- final schema research/redesign last.

## Continuity maintenance obligation

After every load-bearing change:
- append Design Thread Stream;
- update Live Shadow;
- update Current/Next/Doctrine/Revisit/Trace as applicable;
- checkpoint at thread rollover/promotion/recovery boundaries;
- read back manifest/hash after mutation.

Next event that makes this snapshot stale: checklist reconciliation changes the genuine-open frontier, full-suite state changes, live promotion occurs, or tool/capability counts change.