# AI-for-AI Co-Processor Campaign

## Intent
Local core is an epistemic first responder/co-processor for remote agents: spend persistence/locality on state reconstruction, scars, semantic distinctions, evidence currentness, breadth and discriminator preparation; reserve remote-agent intelligence for novel judgment/operator collaboration. Advisory != authority.

## A1 — Can local advisory compress evidence without manufacturing authority?
PASS. `agent_advisory.py` is deterministic/authority-neutral, bounded, preserves UNKNOWN/contradictions, reopen conditions and evidence scaffolding class; chooses HELIX-like successor only when declared discrimination/cost inputs exist; missing scores remain None. `may_mutate=false`, `may_promote=false`, `authority=NONE`.

Combined advisory + governance-relations + operator-authority warfort: 26/26 PASS.

## A2 — Real scar replay: ACTIVE + public lease_id=null
PASS at advisory-selection level. Given server-enforced facts about persisted lease_id_hash, acquire-only bearer disclosure and inspect expiry, plus the prior false-corruption scar, advisory selects `inspect-public-vs-persisted-semantics` over `add-maintenance-reconcile` as sharper discriminator and preserves exact reopen condition. This would have skipped much of the earlier spitballing.

Evidence ceiling: observations/candidates were supplied to the advisory. The local core does not yet autonomously gather them from Live Shadow/RES/source/runtime or generate candidate hypotheses. Therefore this is ADVISORY_COMPOSITION_PROVEN, not AUTONOMOUS_FIRST_RESPONDER_PROVEN.

## Next sharpest discriminator
Wire a read-only evidence gatherer over existing local surfaces (Live Shadow/DTS/RES, governance witnesses, qualification lifecycle, capability catalog/effect profile, result handles, current project/Git/runtime state) that produces provenance-bearing observations and scars for advisory. Then test one anomaly where the remote agent supplies only issue text + project_id. The core must gather bounded evidence, expose missing evidence rather than guess, and return a discriminator. After deterministic gatherer is proven, evaluate whether local model inference adds value for candidate-hypothesis generation without becoming authority.

## Force-multiplication law
LOCAL AI SHOULD COMPRESS SEARCH COST, NOT DUPLICATE REMOTE CHAT. LOCAL INFERENCE MAY PROPOSE; SERVER EVIDENCE QUALIFIES; REMOTE/OPERATOR JUDGMENT GOVERNS CONSEQUENCE.
