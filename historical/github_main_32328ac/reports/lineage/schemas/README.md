# Historical Custom GPT schemas

These schemas are retained only for lineage and comparison. They are not runtime inputs and must not be imported into the current Custom GPT.

The authoritative V29 schema is:

`baseline/pcmmad_receiver/pcmmad_lab_action_schema_v10_3_pcmmad_native_protocol_compact_30_router.json`

Historical server URLs are preserved as evidence of the prior deployment and are not active configuration.
